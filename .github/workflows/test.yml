name: Test

on:
  push:
    paths:
      - src/**
  pull_request:
    paths:
      - src/**

jobs:
  run-test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Run Tests
    - run: cargo test --verbose
